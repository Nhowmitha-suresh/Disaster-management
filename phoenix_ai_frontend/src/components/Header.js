import React from 'react';

const Header = () => (
  <header style={{background: '#1e293b', color: '#fff', padding: '1rem'}}>
    <h1>Phoenix AI Disaster Management</h1>
  </header>
);

export default Header;
